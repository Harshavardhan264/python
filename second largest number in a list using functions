def f(num_list):
	
	largest=max(num_list)
	num_list.remove(largest)
	second_largest=max(num_list)
	return second_largest
num_list=[12,32,6,23,84]
second_largest=f(num_list)
print("second largest number:",second_largest)
